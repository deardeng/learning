1、写一个分数类，尝试支持＋－＊／运算符。注意它们都应该返回一个临时的分数对象。
2、写一个窗口类，创建时提供标题、位置和尺寸，这几个都有默认值。要求提供如下成员函数：设置/取得窗口标题，设置/取得窗口位置，设置/取得窗口尺寸，移动窗口。建议位置和结构各自定义一个类或者结构来表示。
3、写一个文件类，在构造函数中提供文件名并且打开文件，析构函数中负责关闭文件。要求提供如下成员函数：显示文件内容，从文件中读取一行文字，从文件读取一个字符。