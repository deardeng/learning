1、写一个复数类Complex，(复数形如3.2+5.6i,2.9-1.3i,其中i*i=-1)。要求支持＋－＊／，++、--，到bool类型和string类型的转换，支持>>、<<运算符。
2、写一个字符串类String，要求可以用一个C风格字符串来初始化它，支持＋、＋＝、＝＝、！＝、<<、>>、[]，提供size()和clear()函数。注意三大函数。
3、写一个大数类，能处理50位以内的整数的加减运算、赋值运算、和输入输出。